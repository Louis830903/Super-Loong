# 🚀 Super Agent 超级安装部署指南

> **保姆级教程 — 手把手教你装好 Super Agent，零基础也能看懂**

本指南假定你有一台 Windows 电脑，没有编程经验。每一条命令都附带解释，你只需要复制粘贴。

---

## 目录

- [⏱️ 时间预估](#-时间预估)
- [📋 事前准备](#-事前准备)
  - [第一步：安装 Node.js](#第一步安装-nodejs)
  - [第二步：安装 Git](#第二步安装-git)
  - [第三步：安装 Python（可选）](#第三步安装-python可选)
  - [第四步：获取一个大模型 API Key](#第四步获取一个大模型-api-key)
- [📥 下载项目](#-下载项目)
- [⚙️ 安装依赖](#️-安装依赖)
- [🔐 配置密钥](#-配置密钥)
- [🏗️ 构建项目](#️-构建项目)
- [🚀 启动服务](#-启动服务)
- [🌐 访问使用](#-访问使用)
- [🛠️ 日常管理](#️-日常管理)
- [🆘 常见问题](#-常见问题)
- [🗑️ 卸载方法](#️-卸载方法)

---

## ⏱️ 时间预估

| 步骤 | 耗时 | 说明 |
|------|:---:|------|
| 安装 Node.js + Git | 5~15 分钟 | 第一次需要下载安装包 |
| 下载项目 | 1~5 分钟 | 取决于网速 |
| 安装依赖 | 3~10 分钟 | `pnpm install` 自动完成 |
| 配置密钥 | 2~5 分钟 | 获取 API Key + 填写 .env.production |
| 构建项目 | 1~3 分钟 | `pnpm build` |
| 启动服务 | 30 秒 | `start.bat` 一键搞定 |

> 💡 **总计约 15~40 分钟**，大部分时间花在下载上，你只需要操作几次复制粘贴。

---

## 📋 事前准备

### 第一步：安装 Node.js

Node.js 是程序运行的"引擎"，就像汽车需要发动机一样。

#### 1.1 下载安装包

打开浏览器，访问：**https://nodejs.org**

你会看到两个绿色的大按钮：
- 左边是 **LTS**（推荐，稳定版）— 选这个
- 右边是 **Current**（最新版）

**点击左边的 LTS 按钮下载。**

![下载 Node.js](https://nodejs.org/static/images/logo.svg)

> 📌 如果你在国内，访问 nodejs.org 可能比较慢，也可以用国内镜像：
> - 淘宝镜像：https://npmmirror.com/mirrors/node/
> - 选择 `v20.x.x` 或更高版本，下载 `node-v20.x.x-x64.msi`

#### 1.2 安装

1. 双击下载的 `.msi` 安装包
2. 一路点 **Next**（下一步），全部默认选项即可
3. 安装完成后，点 **Finish**（完成）

#### 1.3 验证安装

按键盘 `Win + R`，输入 `powershell`，回车。

在弹出的蓝色/黑色窗口中输入：

```powershell
node --version
```

如果看到类似 `v20.11.0` 的输出，说明安装成功！

再验证包管理器：

```powershell
npm --version
```

应该显示类似 `10.x.x` 的版本号。

#### 1.4 安装 pnpm（更快的包管理器）

Super Agent 使用 `pnpm` 管理依赖，比 npm 快 3 倍：

```powershell
npm install -g pnpm
```

> 🔍 这行命令的意思：用 npm 全局安装（-g）pnpm 这个工具。

验证安装：

```powershell
pnpm --version
```

看到版本号（如 `9.x.x`）就说明好了。

---

### 第二步：安装 Git

Git 是用来下载项目代码的工具。

#### 2.1 下载

访问：**https://git-scm.com/download/win**

点击页面中央的下载链接（一般会自动开始下载 64 位版本）。

> 📌 国内镜像：https://npmmirror.com/mirrors/git-for-windows/

#### 2.2 安装

1. 双击下载的安装包
2. **一路点 Next**，全部保持默认设置（不用改任何选项）
3. 安装完成后点 Finish

#### 2.3 验证

```powershell
git --version
```

看到 `git version 2.x.x` 就成功了。

---

### 第三步：安装 Python（可选）

> ⚠️ **如果你不需要 IM 网关（飞书/钉钉/企微）、视频生成、知识库文档解析，可以跳过这一步。**

Python 用于运行 IM 网关等微服务。

#### 3.1 下载

访问：**https://www.python.org/downloads/**

点击黄色的 **Download Python 3.x.x** 按钮。

> 📌 国内镜像：https://npmmirror.com/mirrors/python/

#### 3.2 安装

1. 双击安装包
2. ⚠️ **重要！勾选底部的 "Add Python to PATH"**（把 Python 加入系统路径）
3. 点击 **Install Now**
4. 安装完成后关闭

#### 3.3 验证

```powershell
python --version
```

显示 `Python 3.x.x` 即成功。

---

### 第四步：获取一个大模型 API Key

Super Agent 需要调用大模型来思考和回答问题，你需要一个 API Key（相当于调用大模型服务的"钥匙"）。

#### 推荐：阿里 DashScope（国内最方便）

1. 打开 **https://dashscope.aliyun.com**
2. 用支付宝/淘宝账号扫码登录
3. 点击右上角头像 → **API-KEY 管理**
4. 点击 **创建 API-KEY**
5. **复制并保存这个 Key**（格式类似 `sk-xxxxxxxxxxxxxxxx`）

> 🔒 **重要**：这个 Key 就像银行卡密码，不要告诉别人，不要截图发网上。

#### 其他选择

| 提供商 | 获取地址 | 说明 |
|--------|----------|------|
| DeepSeek | https://platform.deepseek.com | 价格极低 |
| 智谱 GLM | https://open.bigmodel.cn | 国产模型 |
| Kimi | https://platform.moonshot.cn | 长上下文 |
| OpenAI | https://platform.openai.com | 需国外信用卡 |

---

## 📥 下载项目

### 方式一：GitHub 克隆（推荐，无需登录）

```powershell
# ① 进入你希望存放项目的目录（比如 D 盘的 Projects 文件夹）
cd D:\Projects

# ② 从 GitHub 克隆项目（公开仓库，不需要登录）
git clone https://github.com/Louis830903/Super-Loong.git

# ③ 进入项目目录
cd Super-Loong
```

> 📌 **GitHub 是项目的官方仓库，公开可访问，不需要输入用户名密码。**

### 方式二：Gitee 克隆（国内速度更快，可能需要登录）

> ⚠️ **注意**：Gitee 自 2023 年起已禁用密码登录。如果仓库是私有的，`git clone` 会提示输入用户名和密码——**此时密码不能用，必须使用私人令牌（Personal Access Token）代替**。

**如果你遇到"请输入用户名密码"的提示，有二种解决方式：**

**A. 直接用 GitHub（推荐，省事）**——切回方式一，不需要任何认证。

**B. 去 Gitee 生成私人令牌：**

1. 打开 https://gitee.com/profile/personal_access_tokens
2. 点击「生成新令牌」
3. 随便填个名称（如 `git-clone`），权限默认即可
4. 点击「提交」，**复制生成的令牌**（格式类似 `gitee-xxxxxxxxxxxxxxxxxxxx`）
5. 回到 PowerShell，在提示输入密码时**粘贴这个令牌**（不是你的 Gitee 密码！）

```powershell
# Gitee 克隆（按 Gitee 官方指引）
git clone https://gitee.com/lv--dapang/super-loong.git
# 用户名：输入 lv--dapang
# 密码：粘贴上面生成的私人令牌（不会显示在屏幕上，正常）
```

### 方式三：直接下载 ZIP（完全不需要 Git）

如果以上方式都不方便，可以：

1. 浏览器打开 https://github.com/Louis830903/Super-Loong
2. 点击绿色 **<> Code** 按钮 → **Download ZIP**
3. 解压到你想要的目录（如 `D:\Projects\Super-Loong`）
4. 在 PowerShell 中 `cd` 进入解压后的目录继续后续步骤

> 🔍 解释：
> - `cd` = change directory（切换目录）
> - `git clone` = 从网络下载项目代码
> - 下载后会在 `D:\Projects\Super-Loong` 创建一个文件夹

---

## ⚙️ 安装依赖

### ⚠️ 前置检查：确认 pnpm 已安装

在安装依赖之前，先确认 pnpm 工具可用：

```powershell
pnpm --version
```

- ✅ **如果显示版本号（如 `9.x.x`）** → 跳过下面的安装步骤，直接进入「安装 Node.js 依赖」
- ❌ **如果提示「无法识别」** → 执行下面这条命令安装：

```powershell
npm install -g pnpm
```

> 🔍 为什么需要这一步？许多用户安装 Node.js 后直接跳到下载项目，忘了装 pnpm。这里兜底检查一下，省得后面报错。

### 安装 Node.js 依赖

仍在项目目录下（`D:\Projects\Super-Loong`），执行：

```powershell
# 安装 Node.js 依赖（这一步可能需要 3~10 分钟，取决于网速）
pnpm install
```

你会看到进度条滚过，最后显示类似 `Done in 45s` 即成功。

> 💡 **如果很慢怎么办？** 可以配置国内镜像加速：
> ```powershell
> pnpm config set registry https://registry.npmmirror.com
> pnpm install
> ```

> ⚠️ **安装完成后如果看到 `ERR_PNPM_IGNORED_BUILDS` 警告**（提示 Ignored build scripts），这是 pnpm 的安全机制，需要手动批准编译：
> ```powershell
> pnpm approve-builds
> ```
> 在交互界面中，用**空格键勾选** `cpu-features`、`electron`、`ssh2` 这三个包，然后回车。
> 这三个包分别是：Electron 桌面面板、SSH 远程沙箱及其底层依赖，不批准会导致对应功能无法使用。
>
> ⚡ **如果 Electron 编译报错 `ECONNRESET`**（国内下载 GitHub 超时），设置国内镜像后重新安装：
> ```powershell
> $env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
> pnpm install
> ```

### 安装 Python 依赖（如果需要 IM 网关/视频/文档解析）

```powershell
cd services/im-gateway
pip install uv          # 先安装 uv（比 pip 更快）
uv sync                 # 安装所有 Python 依赖
cd ../..                # 返回项目根目录
```

---

## 🔐 配置密钥

### 5.1 生成必需的加密密钥

项目需要两个密钥来保护数据安全，在 PowerShell 中生成：

```powershell
# ① 生成 JWT 密钥（复制输出的 128 位字符串）
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# ② 生成加密密钥（复制输出的 64 位字符串）
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

把上面输出的两串字符**复制保存下来**，下一步要用。

### 5.2 填写配置文件

```powershell
# 用记事本打开生产环境配置文件
notepad .env.production
```

在打开的记事本中，找到以下几行，把 `=` 后面填上：

```ini
# 粘贴第一步生成的 128 位字符串
JWT_SECRET=这里粘贴刚才生成的第一串密钥

# 粘贴第二步生成的 64 位字符串
SA_ENCRYPTION_KEY=这里粘贴刚才生成的第二串密钥

# 粘贴你从 DashScope 获取的 API Key
LLM_API_KEY=sk-这里粘贴你的API-Key
```

> 📝 **示例**（不要照抄，用你自己的！）：
> ```ini
> JWT_SECRET=a1b2c3d4e5f6...（128位）
> SA_ENCRYPTION_KEY=1a2b3c4d5e6f...（64位）
> LLM_API_KEY=sk-1234567890abcdef
> ```

修改完后 **Ctrl+S 保存**，然后关掉记事本。

---

## 🏗️ 构建项目

在 PowerShell 中执行（仍在项目目录下）：

```powershell
pnpm build
```

这会编译项目代码，大概需要 1~3 分钟。看到类似输出即成功：

```
✓ Compiled successfully
✓ Built in 45.2s
```

> ⚠️ 如果看到 `Next.js build worker exited with code: 1` 但命令最终完成——这是正常现象，不影响使用。我们已经确认 API 核心包编译成功。

---

## 🚀 启动服务

### 一键启动（推荐）

在项目目录下执行：

```powershell
.\start.bat
```

程序会自动：
1. ✅ 检查并安装 PM2（进程守护工具）
2. ✅ 检查构建产物是否存在
3. ✅ 启动 API 服务 + 前端页面
4. ✅ 配置开机自启（下次开机自动运行）

看到以下界面说明启动成功：

```
╔══════════════════════════════════════════╗
║     Super Agent 生产模式已启动！         ║
╚══════════════════════════════════════════╝

   管理面板: http://localhost:3000
   API 服务:  http://localhost:3001
   IM 网关:   http://localhost:8642
```

> 💡 首次运行会自动全局安装 PM2，请确保网络畅通。

### 手动启动（备选）

```powershell
# 安装 PM2（如未安装）
npm install -g pm2

# 启动服务
pm2 start ecosystem.config.cjs --env production

# 保存进程列表 + 配置开机自启
pm2 save
pm2 startup
```

---

## 🌐 访问使用

打开你的浏览器，输入以下地址：

| 服务 | 地址 | 说明 |
|------|------|------|
| 🏠 **管理面板** | http://localhost:3000 | 对话聊天、Agent 管理、知识库 |
| 🔌 **API 服务** | http://localhost:3001 | 后端接口 |
| 🌉 **IM 网关** | http://localhost:8642 | 飞书/钉钉/企微消息接入 |

> 💡 **首次打开管理面板**：会进入一个对话界面，直接输入问题就能和 AI 对话了！

---

## 🛠️ 日常管理

### 查看运行状态

```powershell
.\start.bat status
# 或者
pm2 status
```

正常状态会显示：

| 名称 | 状态 | CPU | 内存 |
|------|:---:|-----|------|
| super-agent-api | online | 2% | 150MB |
| super-agent-web | online | 1% | 80MB |

### 查看日志

```powershell
.\start.bat logs          # 实时查看
pm2 logs super-agent-api  # 只看 API 日志
```

也可直接打开日志文件：

```powershell
# 用记事本打开日志
notepad logs/pm2-api-out.log
```

### 重启服务

```powershell
.\start.bat restart
# 或者
pm2 restart all
```

### 停止服务

```powershell
.\start.bat stop
# 或者
pm2 stop all
```

### 快捷命令（npm scripts）

```powershell
npm run start:prod    # 启动
npm run stop:prod     # 停止
npm run restart:prod  # 重启
npm run status:prod   # 查看状态
npm run logs:prod     # 查看日志
```

### 开机自启

默认已配置。如需取消：

```powershell
pm2 unstartup
```

### 更新程序

当有新版本时：

```powershell
# 拉取最新代码
git pull

# 重新安装依赖（如有新增）
pnpm install

# 重新构建
pnpm build

# 重启服务
.\start.bat restart
```

---

## 🆘 常见问题

### Q0：`git clone` Gitee 提示"请输入用户名密码"

**原因**：Gitee 自 2023 年起已禁用密码登录，私有仓库 clone 必须使用私人令牌（Personal Access Token），不能用密码。

**解决（三选一，推荐 A）**：

**A. 直接用 GitHub（无需任何认证）**：
```powershell
cd D:\Projects
git clone https://github.com/Louis830903/Super-Loong.git
```

**B. 生成 Gitee 私人令牌代替密码**：
1. 打开 https://gitee.com/profile/personal_access_tokens
2. 生成新令牌 → 复制
3. 克隆时用户名填 `lv--dapang`，密码粘贴令牌：
```powershell
git clone https://gitee.com/lv--dapang/super-loong.git
```

**C. 直接下载 ZIP（完全不需要 Git）**：
1. 浏览器打开 https://github.com/Louis830903/Super-Loong
2. 点绿色 Code 按钮 → Download ZIP → 解压即可

---

### Q1：`pnpm` 命令找不到

**原因**：pnpm 未安装或安装路径不在系统环境变量中。

**解决**：
```powershell
npm install -g pnpm
```
如果仍然不行，关闭 PowerShell 重新打开，再试一次。

---

### Q2：`pnpm install` 报网络错误

**原因**：npm 默认源在国外，国内访问可能超时。

**解决**：
```powershell
pnpm config set registry https://registry.npmmirror.com
pnpm install
```

---

### Q3：启动后浏览器打开 localhost:3000 显示"无法访问"

**可能原因和解决**：

1. **服务还没完全启动**——等 30 秒再刷新试试
2. **端口被占用**——检查：
   ```powershell
   netstat -ano | findstr :3000
   ```
   如果有输出，说明端口被占用。用 `taskkill /PID <PID号> /F` 杀掉占用的进程。
3. **检查 PM2 状态**：
   ```powershell
   pm2 status
   ```
   如果显示 `errored` 或 `stopped`，查看日志：
   ```powershell
   pm2 logs super-agent-api
   ```

---

### Q4：提示"JWT_SECRET 未设置"

**原因**：`.env.production` 中的 `JWT_SECRET=...` 没有填写。

**解决**：
```powershell
notepad .env.production
```
找到 `JWT_SECRET=`，在 `=` 后面填入生成的密钥。

---

### Q5：提示"API Key 无效"

**原因**：LLM_API_KEY 填写错误或额度用完。

**解决**：
- 检查 `.env.production` 中的 `LLM_API_KEY=` 是否正确
- 登录对应平台查看 API Key 余额和状态
- DashScope 新用户有免费额度，用完需要充值

---

### Q6：IM 网关启动失败（端口 8642）

**原因**：旧进程残留或 Python 环境问题。

**解决**：
```powershell
# 查看端口占用
netstat -ano | findstr :8642

# 杀掉占用进程
taskkill /PID <PID号> /F

# 或者直接禁用 IM 网关（如果不需要）
notepad .env.production
# 取消注释这一行：
DISABLE_IM_GATEWAY=true
```

---

### Q7：如何确认服务正在运行？

```powershell
# 方式 1：PM2 状态
pm2 status

# 方式 2：API 健康检查
curl http://localhost:3001/api/system/health
# 返回 {"status":"ok"} 即正常

# 方式 3：浏览器打开
# http://localhost:3000
```

---

### Q8：电脑重启后服务会自动启动吗？

**会的**。`start.bat` 首次运行时已经通过 `pm2 startup` 配置了开机自启。

验证方式：
```powershell
pm2 status
```
重启电脑后执行上述命令，如果服务状态是 `online` 就说明成功了。

---

### Q9：如何更换大模型？

编辑 `.env.production`：

```powershell
notepad .env.production
```

修改这几行：
```ini
# 切换到 DeepSeek
LLM_PROVIDER=deepseek
LLM_MODEL=deepseek-chat
LLM_API_KEY=sk-your-deepseek-key
LLM_BASE_URL=https://api.deepseek.com/v1
```

修改后保存，重启服务：
```powershell
.\start.bat restart
```

---

### Q10：项目占用多少磁盘空间？

| 内容 | 大小 |
|------|------|
| 项目代码 | ~50MB |
| Node.js 依赖 (node_modules) | ~800MB |
| PM2 + 日志 | ~50MB |
| 数据文件（数据库、配置等） | ~10~100MB |
| **合计** | **约 1GB** |

---

## 🗑️ 卸载方法

如果想彻底卸载：

```powershell
# 1. 停止并删除 PM2 进程
pm2 stop all
pm2 delete all
pm2 unstartup

# 2. 卸载 PM2
npm uninstall -g pm2 pm2-windows-startup

# 3. 删除项目文件夹（在资源管理器中删除即可）
# D:\Projects\Super-Loong

# 4. （可选）卸载 Node.js / Git / Python
# 在 Windows "设置" → "应用" 中卸载
```

---

## 📞 获取帮助

- **项目主页**：https://github.com/Louis830903/Super-Loong
- **问题反馈**：https://github.com/Louis830903/Super-Loong/issues
- **开发文档**：项目目录下的 `REPO-WIKI.md`

---

> 🎉 **恭喜！你已经完成了 Super Agent 的安装部署。现在打开 http://localhost:3000 开始使用吧！**
