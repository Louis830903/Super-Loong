# ====== 依赖与构�?======
node_modules/
dist/
.next/
coverage/
.turbo/
*.tsbuildinfo

# ====== v3 Task 6 OpenAPI codegen 中间产物 ======
# zod schema 生成的 openapi.json 是构建中间件，每次 gen:openapi 都会重生成，不入库；
# 但 packages/web-types/src/generated.ts 必须入库（gen:types:check 漂移闸门基线）
packages/api/openapi/openapi.json

# ====== 环境配置（含密钥）======
# v3 Task 0a 纵深防御：所有 .env.* 默认忽略，仅放行 .env.example 模板
.env
.env.local
.env.production
.env.staging
.env.*.local
!.env.example

# ====== 凭据保险柜（v3 Task 0a 新增）======
# 主密钥/单文件保险柜数据，绝不入库；example 模板放行
vault/
!vault/vault.example.json
*.vault.json
*.master.key
*.pem
*.p12
*.pfx

# ====== 数据库文件 ======
*.db
*.db-journal
# better-sqlite3 WAL 模式辅助文件（不应提交到版本控制）
*.db-wal
*.db-shm
*.db.bak
*.bak
data/super-agent.db
data/super-agent.db.bak
data/backups/
data/cache/
data/logs/
data/sessions/

# IM 网关渠道凭据配置（含 app_id/app_secret 等敏感字段）
services/im-gateway/data/*.json

# ====== 会话记录（含用户聊天数据）======
packages/api/data/sessions/
data/sessions/

# ====== 个人测试文件 ======
*.mp3
*.png
*.xlsx

# 项目静态资源（排除 *.png 规则）
!packages/web/public/logo.png
test-api.mjs
tsc_*.txt
check_mic.ps1
信息收集表单.html
精酿啤酒电商销售策略分析报告.md

# ====== Python ======
__pycache__/
*.pyc
*.pyo
.venv/
.pytest_cache/

# ====== 内部工具配置 ======
.qoder/
.graphifyignore
graphify-out/
package-lock.json

# ====== 系统文件 ======
.DS_Store
*.log
*.zip
Thumbs.db

# ====== Video-Forge 运行时产物（便携版 ffmpeg + 调试快照）======
# 注意：必须用 / 开头的绝对根匹配，否则会误伤 packages/core/src/runtime/（bootstrap.ts 所在目录）
# /runtime/ 由 bootstrap.ts 按需下载 ffmpeg/ffprobe 便携版（数百 MB），并堆放 curl 响应快照
/runtime/
# 根级临时请求/响应文件（手工 curl / vitest 输出转储）
tmp_*.json
vitest_output.txt
# 数据库带版本后缀的备份（*.bak 规则不覆盖 *.db.bak.pre-v2-fix 等派生文件）
*.db.bak.*
data/super-agent.db.bak.*
# services/video-forge 子服务的本地数据目录（用户生成的临时媒体 + SQLite）
services/video-forge/data/
services/video-forge/output/
services/video-forge/.venv/
services/video-forge/__pycache__/
