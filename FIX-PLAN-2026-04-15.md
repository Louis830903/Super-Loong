# Super Agent 平台全面修复 Spec 计划

> 审查日期：2026-04-15
> 基于 OpenClaw（执行安全层参考）、Hermes（记忆+进化+并发参考）最佳实践
> 结合本轮代码审查发现的 84 个问题

---

## 调研发现：最佳实践对标摘要

| 问题域 | OpenClaw 方案 | Hermes 方案 | Super Agent 现状 | 采纳方案 |
|--------|-------------|------------|-----------------|---------|
| **会话并发保护** | N/A (单用户CLI) | `_AGENT_PENDING_SENTINEL` 哨兵锁 + `_running_agents` Map + stale eviction + `contextvars` 隔离 | ❌ 完全无保护 | **Hermes**: per-session Promise锁 + 哨兵模式 |
| **命令执行安全** | `isSafeExecutableValue()` 多层校验(null/控制字符/shell元字符/引号) + `sanitizeEnvVars()` 模式黑名单 + `validateSandboxSecurity()` + `strictInlineEval` 白名单 | `check_all_command_guards()` 审批流 | ❌ SSH直接拼接 / MCP无白名单 | **OpenClaw**: 命令白名单 + 安全校验函数 |
| **环境变量泄漏** | `sanitizeHostExecEnv()` 清洗 + `sanitizeEnvVars()` 模式匹配 + blocked/warnings 分级 | `HERMES_REDACT_SECRETS` 导入时快照 | ❌ `process.env` 全量传递 | **OpenClaw**: 最小化环境变量传递 |
| **MCP认证** | Bearer Token + `x-session-key` header + loopback server 隔离 | N/A | ⚠️ 有auth配置但DB不持久化 | **OpenClaw**: Token认证 + 持久化 |
| **记忆持久化** | 文件快照 `memory/YYYY-MM-DD.md` | `MEMORY.md/USER.md` + `fcntl.flock` 文件锁 + frozen snapshot (prefix cache不变) + 字符限制 | ⚠️ 已有MarkdownMemory但核心记忆块纯内存 | **Hermes**: 文件锁 + 冻结快照 + 磁盘持久化 |
| **记忆检索** | N/A | Holographic: trust score + HRR向量 + entity resolution; Hindsight: 知识图谱 + prefetch lock | ⚠️ InMemory embedding评分=0 | **Hermes**: 混合检索 + limit分页 |
| **FTS查询安全** | N/A | N/A | ❌ 用户输入直接MATCH | **通用**: 双引号转义 |
| **会话DB健壮** | N/A | `INSERT OR IGNORE` 幂等 + `ensure_session` 崩溃恢复 + 30s SQLite timeout | ⚠️ 部分事务化 | **Hermes**: 幂等+超时+恢复 |
| **进化引擎** | N/A | Hindsight: 后台retain + turn batching + `_prefetch_lock` 线程安全 | ⚠️ proposals Map无上限 | **Hermes**: 容量限制 + 后台异步 |
| **JWT安全** | N/A | N/A | ❌ 硬编码默认secret | **通用**: 生产强制+启动拒绝 |
| **CORS/认证** | N/A | N/A | ❌ IM网关全开放 | **通用**: 白名单+API Key |

---

## 问题统计汇总

| 严重等级 | 数量 | 分布 |
|---------|------|------|
| **P0 致命** | **13** | 安全漏洞(8)、并发竞态(2)、数据损坏(2)、注入(1) |
| **P1 重要** | **37** | 逻辑缺陷(12)、安全(5)、性能(6)、资源泄漏(5)、功能缺失(9) |
| **P2 改进** | **34** | 代码冗余(10)、类型安全(6)、风格(8)、配置(10) |

---

## Phase A：P0 安全紧急修复（13项）

### A-1｜JWT Secret 生产强制校验
- **文件**: `packages/api/src/auth/index.ts`
- **位置**: 第147行
- **参考**: 通用安全最佳实践
- **改动**:
  ```typescript
  const jwtSecret = process.env.JWT_SECRET;
  if (!jwtSecret && process.env.AUTH_ENABLED === "true") {
    throw new Error("[FATAL] JWT_SECRET must be set when AUTH_ENABLED=true");
  }
  if (!jwtSecret) {
    logger.warn("⚠️  JWT_SECRET not set — using insecure default. DO NOT USE IN PRODUCTION!");
  }
  ```

### A-2｜MCP Server 注册命令白名单
- **文件**: `packages/api/src/routes/mcp.ts`
- **位置**: 第30-71行 POST /api/mcp/servers
- **参考**: OpenClaw `isSafeExecutableValue()` — 校验null字节、控制字符、shell元字符、引号
- **改动**: 新增 `isSafeCommand(command: string): boolean` 函数:
  - 白名单允许: `npx`, `node`, `docker`, `python`, `python3`, `uvx`, `deno`, `bun`
  - 校验: 无null字节、无控制字符`[\x00-\x1f]`、无shell元字符`[;|&$<>\`\n]`、无引号
  - args 数组中每个元素同样校验无shell元字符
  - 不在白名单或校验不通过时返回 `400 { error: "Blocked command" }`

### A-3｜SSH Sandbox 命令注入修复
- **文件**: `packages/core/src/security/ssh-sandbox.ts`
- **位置**: 第145-157行 `buildCommand()`
- **参考**: OpenClaw `buildRemoteCommand()` — 所有代码通过单引号转义传递给 `/bin/sh -c`
- **改动**: shell 模式也使用单引号转义:
  ```typescript
  function shellEscape(s: string): string {
    return "'" + s.replace(/'/g, "'\\''") + "'";
  }
  // shell 分支:
  return `${mkdirCmd} && cd ${this.config.workDir} && /bin/sh -c ${shellEscape(code)}`;
  ```

### A-4｜calculate 工具代码注入修复
- **文件**: `packages/core/src/tools/system.ts`
- **位置**: 第85-107行 `calculateTool`
- **参考**: OpenClaw `strictInlineEval` 白名单模式
- **改动**: 替换 `new Function` 为严格白名单表达式求值:
  ```typescript
  const SAFE_EXPRESSION = /^[\d\s+\-*/().%,e]+(Math\.\w+\([\d\s+\-*/().%,e]*\))*$/;
  if (!SAFE_EXPRESSION.test(expression)) {
    return { output: "Error: Expression contains unsafe characters" };
  }
  ```
  或引入 `mathjs` 库的 `evaluate()` 函数（带沙箱模式）。

### A-5｜凭证/安全端点添加鉴权
- **文件**: `packages/api/src/routes/security.ts`
- **位置**: 全局路由注册
- **参考**: OpenClaw Gateway 认证中间件
- **改动**:
  - 为 `POST/DELETE /api/security/credentials` 添加 `preHandler: requirePermission("security:write")`
  - 为 `GET /api/security/credentials` 添加 `preHandler: requirePermission("security:read")`
  - 当 `AUTH_ENABLED=false` 时，限制凭证端点仅允许 localhost 访问

### A-6｜WebSocket 广播端点添加认证
- **文件**: `packages/api/src/ws/index.ts`
- **位置**: 第216-223行 `POST /api/ws/broadcast`
- **改动**: 添加内部请求验证（检查 `x-internal-key` header 或限制仅 localhost）

### A-7｜IM 网关 CORS 收紧 + API Key 认证
- **文件**: `services/im-gateway/server.py`
- **位置**: 第175-180行 CORS 配置 + 全局中间件
- **参考**: OpenClaw Bearer Token 认证模式
- **改动**:
  ```python
  # CORS 白名单
  cors_origins = os.environ.get("CORS_ORIGINS", "http://localhost:3001").split(",")
  app.add_middleware(CORSMiddleware, allow_origins=cors_origins, ...)

  # API Key 中间件
  IM_GATEWAY_API_KEY = os.environ.get("IM_GATEWAY_API_KEY", "")
  @app.middleware("http")
  async def verify_api_key(request, call_next):
      if request.url.path in ("/health", "/docs", "/openapi.json"):
          return await call_next(request)
      if IM_GATEWAY_API_KEY and request.headers.get("X-API-Key") != IM_GATEWAY_API_KEY:
          return JSONResponse(status_code=401, content={"error": "Invalid API key"})
      return await call_next(request)
  ```
  - 同步修改 `packages/api/src/routes/channels.ts` 中代理请求携带 `X-API-Key` header

### A-8｜FTS5 查询注入防护
- **文件**: `packages/core/src/persistence/sqlite.ts`
- **位置**: 第1161, 1224, 1454行 — `MATCH ?` 查询
- **参考**: 通用 FTS5 安全实践
- **改动**: 新增 `escapeFTS5Query` 工具函数:
  ```typescript
  function escapeFTS5Query(query: string): string {
    return query
      .split(/\s+/)
      .filter(Boolean)
      .map(word => `"${word.replace(/"/g, '""')}"`)
      .join(' ');
  }
  ```
  所有 `MATCH ?` 参数使用此函数处理

### A-9｜chat()/chatStream() per-session 并发锁
- **文件**: `packages/core/src/agent/runtime.ts`
- **位置**: 第307-629行
- **参考**: Hermes `_AGENT_PENDING_SENTINEL` 哨兵机制 + `_running_agents` Map + `contextvars` 隔离
- **改动**:
  ```typescript
  private sessionLocks = new Map<string, Promise<void>>();

  private async acquireSessionLock(sessionId: string): Promise<() => void> {
    const prev = this.sessionLocks.get(sessionId) ?? Promise.resolve();
    let release: () => void;
    const lock = new Promise<void>(r => { release = r; });
    this.sessionLocks.set(sessionId, lock);
    await prev;
    return release!;
  }
  ```
  在 `chat()` 和 `chatStream()` 入口处获取锁，finally 中释放

### A-10｜FTS5 触发器列定义统一
- **文件**: `packages/core/src/persistence/sqlite.ts`
- **位置**: 第153-157行 migrateV4 触发器 vs 第516-521行 FTS5 表定义
- **改动**:
  - migrateV4 中先 `DROP TRIGGER IF EXISTS conv_msg_fts_ai, conv_msg_fts_ad`
  - 触发器列统一为 `INSERT INTO conv_messages_fts(rowid, content) VALUES (NEW.id, NEW.content)`
  - 移除 `conversationId` 列引用

### A-11｜chat() 错误回滚 + sanitizeMessages
- **文件**: `packages/core/src/agent/runtime.ts`
- **位置**: 第338-435行
- **参考**: Hermes `run_conversation` — 错误恢复 + transcript完整性
- **改动**:
  ```typescript
  async chat(...) {
    const session = this.getSession(sessionId);
    session.messages = this.sanitizeMessages(session.messages); // 预清洗
    const msgCountBefore = session.messages.length;
    try {
      // ... 原有逻辑
    } catch (error) {
      this._status = "error";
      session.messages.length = msgCountBefore; // 回滚
      throw error;
    }
  }
  ```

### A-12｜OpenAI兼容 SSE 添加 AbortController
- **文件**: `packages/api/src/routes/chat.ts`
- **位置**: 第202-238行 `/v1/chat/completions` stream模式
- **改动**: 参照 `/api/chat/stream`（第141-146行），添加:
  ```typescript
  const abortController = new AbortController();
  reply.raw.on("close", () => abortController.abort());
  // 传递给 runtime.chatStream(..., { signal: abortController.signal })
  ```

### A-13｜MCP Client sendRequest 双重set修复
- **文件**: `packages/core/src/mcp/client.ts`
- **位置**: 第383-401行
- **改动**: 删除第388行的第一次 `pendingRequests.set()`，只保留 timer 创建后的那次

---

## Phase B：P1 正确性与健壮性修复（20项）

### B-1｜前后端字段对齐：Memory 页面
- **文件**: `packages/web/src/app/memory/page.tsx`
- **改动**: `data.entries` → `data.memories`

### B-2｜前后端字段对齐：Agent 创建表单
- **文件**: `packages/web/src/app/agents/page.tsx`
- **改动**: 提交时构建嵌套结构 `{ llmProvider: { type: form.provider, model: form.model } }`

### B-3｜前后端字段对齐：Security 策略类型
- **文件**: `packages/web/src/app/security/page.tsx`
- **改动**: 统一 `SecurityPolicy` 接口字段为后端实际字段 (`defaultSandbox`, `defaultPermission`, `maxConcurrentSandboxes`)

### B-4｜MCPRegistry 返回实际工具列表
- **文件**: `packages/core/src/mcp/registry.ts`
- **位置**: 第118-147行 `listServers()`/`getServer()`
- **改动**: `tools: client?.getRawTools() ?? []`

### B-5｜MCPRegistry loadFromDB 恢复 auth 配置
- **文件**: `packages/core/src/mcp/registry.ts`
- **位置**: 第40-50行
- **参考**: OpenClaw MCP 配置持久化模式
- **改动**: 在 `saveMCPServer`/`loadMCPServers` 中序列化/反序列化 `auth` 字段

### B-6｜MCP HTTP/SSE 请求添加超时
- **文件**: `packages/core/src/mcp/client.ts`
- **位置**: 第181-205, 234-254行
- **参考**: OpenClaw `connectWithTimeout()` — Promise + setTimeout 包装
- **改动**: 所有 `fetch()` 调用添加 `signal: AbortSignal.timeout(15000)`

### B-7｜MCP Client require("zod") 改为 ESM import
- **文件**: `packages/core/src/mcp/client.ts`
- **位置**: 第266行
- **改动**: 将 `const { z } = require("zod")` 移到文件顶部 `import { z } from "zod"`

### B-8｜stream() 添加 fallback 机制
- **文件**: `packages/core/src/llm/provider.ts`
- **位置**: 第221-280行
- **参考**: Hermes `fallback_model` 配置 — 主模型失败自动切换
- **改动**: 在 `stream()` 中添加 try/catch + fallbackProvider 切换逻辑（与 `complete()` 对齐）

### B-9｜stream() 传递 tools 参数
- **文件**: `packages/core/src/llm/provider.ts`
- **位置**: 第262-268行
- **改动**: 构建请求时包含 `tools` 参数

### B-10｜deleteAgent 清理 MemoryManager
- **文件**: `packages/core/src/agent/manager.ts`
- **位置**: 第139-148行
- **改动**: 在 MemoryManager 添加 `clearCoreMemory(agentId)` 方法并在 `deleteAgent` 中调用

### B-11｜memory.search() 添加 limit 限制
- **文件**: `packages/core/src/memory/manager.ts`
- **位置**: 第559行
- **参考**: Hermes Hindsight `recall_max_tokens` + Holographic `limit` 参数
- **改动**: 给 `backend.list(filters)` 添加 `limit: MAX_CANDIDATES` 参数，避免加载全部数据

### B-12｜memory.stats() 用 count 替代全量加载
- **文件**: `packages/core/src/memory/manager.ts`
- **位置**: 第601行
- **改动**: 用三次 `count({ ...filters, type })` 替代 `list()` 全量加载

### B-13｜saveDatabase() 移除 busy-wait
- **文件**: `packages/core/src/persistence/sqlite.ts`
- **位置**: 第628-629行
- **参考**: Hermes SessionDB 30s SQLite timeout — 避免阻塞
- **改动**: 如果必须保持同步，使用 `Atomics.wait()` 替代自旋；或改为异步 `await new Promise(r => setTimeout(r, jitter))`

### B-14｜deleteConversation 事务保护
- **文件**: `packages/core/src/persistence/sqlite.ts`
- **位置**: 第1353-1366行
- **改动**: 包裹 `BEGIN; DELETE messages; DELETE conversations; COMMIT;` 事务

### B-15｜文件系统工具路径遍历保护
- **文件**: `packages/core/src/tools/filesystem.ts`
- **位置**: 第10-47行
- **参考**: OpenClaw `validateSandboxSecurity()` — bind mount 路径策略 + symlink escape hardening
- **改动**: 在 execute 中校验路径不包含 `..` 且在允许的根目录下:
  ```typescript
  const resolved = path.resolve(filePath);
  const allowed = [process.cwd(), paths.home(), os.tmpdir()];
  if (!allowed.some(root => resolved.startsWith(root))) {
    return { output: "Error: Path outside allowed directories" };
  }
  ```

### B-16｜Skill Marketplace SSRF 防护
- **文件**: `packages/core/src/skills/marketplace.ts`
- **位置**: 第119-134行
- **参考**: OpenClaw `sanitizeEnvVars()` 模式匹配 — 白名单优于黑名单
- **改动**: URL 域名白名单:
  ```typescript
  const ALLOWED_HOSTS = [
    "raw.githubusercontent.com",
    "registry.npmjs.org",
    "skillhub.superagent.dev"
  ];
  const url = new URL(sourceUrl);
  if (!ALLOWED_HOSTS.includes(url.hostname) && !url.hostname.endsWith(".github.io")) {
    throw new Error(`SSRF blocked: ${url.hostname} not in allowed hosts`);
  }
  ```

### B-17｜CredentialVault 持久化到 SQLite
- **文件**: `packages/core/src/security/sandbox.ts`
- **位置**: 第357-474行
- **参考**: Hermes Holographic memory `MemoryStore` — SQLite持久化 + 加密列
- **改动**: 当 `persistent === true` 时:
  - `store()` 同时写入 SQLite 表 `credentials(name, encrypted_value, iv, description, allowed_agents, created_at)`
  - 构造时从 SQLite 加载已存在的凭证到内存 Map

### B-18｜Channel 配置持久化
- **文件**: `packages/api/src/routes/channels.ts`
- **位置**: 第17行 `const channels = new Map()`
- **改动**: 使用 SQLite `channels` 表替代内存 Map，启动时 loadFromDB

### B-19｜IM 网关 auto_connect 异常隔离
- **文件**: `services/im-gateway/server.py`
- **位置**: 第116-164行
- **参考**: Hermes Gateway 平台连接隔离 — 各平台独立 try/except
- **改动**: 为每个平台连接添加独立 try/except，一个平台失败不影响其他

### B-20｜EventEmitter listener 清理
- **文件**: `packages/api/src/ws/index.ts`
- **位置**: 第99-107行
- **改动**: 保存 listener 引用，在 `onClose` 中 `off()` 移除

---

## Phase C：P1 性能与体验优化（6项）

### C-1｜SSE 流式更新节流
- **文件**: `packages/web/src/app/chat/page.tsx`
- **位置**: 第480-511行
- **改动**: 使用 `requestAnimationFrame` 或 100ms throttle 批量更新 `setMessages()`

### C-2｜ToolCallCard 移到组件外部
- **文件**: `packages/web/src/app/chat/page.tsx`
- **位置**: 第591-645行
- **改动**: 将 `ToolCallCard` 提取为独立的 `memo()` 包裹组件

### C-3｜提取共享 useAgents hook
- **文件**: 新建 `packages/web/src/hooks/useAgents.ts`
- **改动**: 从 chat/collaboration/cron 等4+页面提取重复的 Agent 列表获取逻辑

### C-4｜进化引擎内存增长限制
- **文件**: `packages/core/src/evolution/engine.ts`
- **位置**: 第342-343行
- **参考**: Hermes Hindsight `retain_every_n_turns` 批量机制 + 容量限制
- **改动**: `proposals` Map 和 `snapshots` 数组添加最大容量限制（如 1000），超出时 LRU 淘汰

### C-5｜协作编排历史限制
- **文件**: `packages/core/src/collaboration/orchestrator.ts`
- **位置**: 第567行
- **改动**: `runHistory` 添加 `maxHistorySize = 100` 配置，超出时删除最老条目

### C-6｜Docker 可用性检查 TTL 缓存
- **文件**: `packages/core/src/security/docker-sandbox.ts`
- **位置**: 第48, 60-74行
- **改动**: 为 `_available` 添加 5 分钟 TTL，过期后重新检查

---

## Phase D：代码清理（10项，保守策略）

> ⚠️ 原则：未使用但可能是"任务未执行到"的功能代码 **不清理**。只清理明确的死代码和冗余代码。

### D-1｜清理明确死代码
- `runtime.ts:631-632` — 已删除方法的废弃注释残留 ✅ 可清理
- `memory/manager.ts:283-288` — `InMemoryBackend.search()` embedding 评分永远为 0 的注释代码 ✅ 可清理（改为 TODO 或直接移除占位代码）

### D-2｜清理未使用导入
- `manager.ts:12` — `PlatformEvent` 未使用 ✅ 可清理
- `dashboard/page.tsx` — `Cpu`, `Wrench` 等未使用图标 ✅ 可清理（import tree-shaking 验证后删除）

### D-3｜清理冗余 getter
- `cron/scheduler.ts:208-214` — `running` 和 `isRunning` 完全相同 ✅ 保留 `isRunning`，给 `running` 加 `@deprecated`

### D-4｜清理重复路径注入
- `services/im-gateway/__init__.py` + `server.py` — HERMES_PATH 注入执行两次 ✅ 保留 `__init__.py` 中的，删除 `server.py` 中的重复

### D-5｜修复 bridge.py 变量遮蔽
- `services/im-gateway/bridge.py:128` — 参数 `text` 被响应覆盖 ✅ 改为 `reply_text`

### D-6｜provider.ts clearTimeout 重复调用
- `provider.ts:178` — try 块中多余的 `clearTimeout` ✅ 移除，保留 finally 中的

### D-7｜channels.ts Date.now() ID 碰撞
- `channels.ts:43` — `Date.now()` 生成ID ✅ 改为 `crypto.randomUUID()`

### D-8｜escapeXml 函数应用
- `prompt/engine.ts:43-50` — `escapeXml()` 已定义但未调用 ⚠️ **不清理**，应该在 L5 层调用它（属于功能增强）

### D-9｜~~未使用的 `requirePermission`~~
- `auth/index.ts:203-218` — ⚠️ **不清理**。这是已实现但未在路由中应用的 RBAC 功能，属于 A-5 修复时会用到的代码

### D-10｜~~CrewTask.async 字段~~
- `orchestrator.ts:54` — ⚠️ **不清理**。这是计划中的并行任务功能，属于"任务未执行到"

---

## Phase E：Schema 验证补全（4项）

### E-1｜collaboration 路由添加 Zod 验证
- **文件**: `packages/api/src/routes/collaboration.ts`
- **改动**: 替换 `req.body as {...}` 为 Zod Schema `safeParse`

### E-2｜evolution 路由添加 Zod 验证
- **文件**: `packages/api/src/routes/evolution.ts`
- **改动**: 同上

### E-3｜cron PUT 路由实现完整更新
- **文件**: `packages/api/src/routes/cron.ts`
- **改动**: 当前只处理 `enabled`，补全 `name`, `expression`, `message`, `timezone` 更新

### E-4｜Zod Schema 与 Interface 同步
- **文件**: `packages/core/src/types/index.ts`
- **位置**: 第278-285行
- **改动**: `AgentConfigSchema` 的 `llmProvider` 补充 `fallback`, `providerId`, `supportsReasoning` 字段

---

## Phase F：类型一致性修复（3项）

### F-1｜LLMMessage.content 类型统一
- **文件**: `packages/core/src/types/index.ts`
- **改动**: `LLMMessage.content` 统一为 `string | null`，配合在 provider.ts 中做空值合并

### F-2｜maxToolIterations 默认值调整
- **文件**: `packages/core/src/types/index.ts`
- **位置**: 第290行
- **参考**: CrewAI=25, Hermes=90 (可配置), LangChain=15
- **改动**: 默认值从 90 降至 **25**（保留用户可配置）

### F-3｜DockerSandbox/SSHSandbox 实现 SandboxBackend 接口
- **文件**: `docker-sandbox.ts`, `ssh-sandbox.ts`
- **改动**: 添加 `implements SandboxBackend`，统一 `execute()` 方法签名

---

## 执行策略

```
Phase A: P0安全修复 (13项)
    ↓
验证: pnpm build + 安全测试
    ↓
Phase B: P1正确性修复 (20项)
    ↓
验证: pnpm build + 前后端联调
    ↓
Phase C: P1性能优化 (6项)
    ↓
Phase D: 代码清理 (10项)
    ↓
Phase E: Schema补全 (4项)
    ↓
Phase F: 类型修复 (3项)
    ↓
验证: 全量 pnpm build + TypeScript strict
```

| Phase | 项数 | 预估工时 | 风险等级 |
|-------|------|---------|---------|
| A: 安全修复 | 13 | 6h | 🔴 高风险-阻断部署 |
| B: 正确性修复 | 20 | 8h | 🟡 中风险-影响功能 |
| C: 性能优化 | 6 | 3h | 🟢 低风险 |
| D: 代码清理 | 10 | 2h | 🟢 低风险 |
| E: Schema补全 | 4 | 2h | 🟢 低风险 |
| F: 类型修复 | 3 | 1h | 🟢 低风险 |
| **合计** | **56** | **~22h** | |

---

## 附录：不清理项目清单

以下项目经审查后判定为"功能未执行到"而非真正的死代码，**保留不动**：

| 项目 | 位置 | 保留理由 |
|------|------|---------|
| `requirePermission()` 装饰器 | `auth/index.ts:203-218` | RBAC 功能已实现，Phase A-5 会应用到路由 |
| `CrewTask.async` 字段 | `orchestrator.ts:54` | 计划中的并行任务编排功能 |
| `escapeXml()` 函数 | `prompt/engine.ts:43-50` | 应在 L5 层 skill 输出时调用，属于功能增强 |
| `SandboxBackend` 接口 | `types/index.ts` | 已有 Docker/SSH 实现引用，Phase F-3 会对齐 |
| `Aliyun NLS Token` 缓存缺失 | `voice/aliyun.ts` | 功能可用但性能待优化，非死代码 |
| `parseNaturalLanguageToCron` | `scheduler.ts:330-350` | 功能可用但模式有限，属于功能增强 |
| `GroupChat auto speaker` | `orchestrator.ts:519-522` | 功能可用但匹配精度待提升 |
| `Docker 环境变量泄漏` | `docker-sandbox.ts:197` | 安全改进但不影响功能正确性 |
| `审计日志持久化` | `sandbox.ts:548` | 重要改进但当前审计功能可用 |
| `PromptEngine 缓存键` | `engine.ts:446-450` | 缓存可能过期但不会崩溃 |
