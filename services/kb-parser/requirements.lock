# KB-Parser Python 依赖锁定文件（知识库 Spec §T7）
#
# 安装方式：uv pip install -r requirements.lock
#
# 裁剪原则：只保留 Docling + FastAPI 必需的直接依赖，间接依赖
# （easyocr、torch、transformers 等）由 docling 自动拉取。

# ── 框架层 ──
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# ── 进阶解析引擎 ──
# IBM Docling 2.x —— 扫描件 OCR / 复杂版式 PDF / 图片文本抽取
docling>=2.0,<3.0

# ── 工具库 ──
loguru>=0.7.0
